(()=>{'use strict';const langButton=document.querySelector('#language');let lang='en';function setLanguage(next){lang=next;document.documentElement.lang=lang;document.querySelectorAll('[data-en]').forEach(el=>{el.innerHTML=el.dataset[lang]});langButton.textContent=lang==='en'?'ES':'EN';langButton.setAttribute('aria-label',lang==='en'?'Cambiar a español':'Switch to English');document.title=lang==='en'?'TwiT — Twitch + Kick Multiview for Fire TV & Android TV':'TwiT — Twitch + Kick y multivista para Fire TV y Android TV';document.querySelector('#copy-status').textContent='';try{localStorage.setItem('twit-site-language',lang)}catch{}}try{const saved=localStorage.getItem('twit-site-language');if(saved==='es'||saved==='en')setLanguage(saved)}catch{}langButton.addEventListener('click',()=>setLanguage(lang==='en'?'es':'en'));document.querySelector('#copy').addEventListener('click',async()=>{const status=document.querySelector('#copy-status');try{await navigator.clipboard.writeText('https://danikdejesus1.github.io/TwiT/');status.textContent=lang==='en'?'Link copied.':'Enlace copiado.'}catch{status.textContent=lang==='en'?'Select and copy the address above.':'Selecciona y copia la dirección de arriba.'}});const motion=matchMedia('(prefers-reduced-motion: reduce)');if('IntersectionObserver'in window&&!motion.matches){document.body.classList.add('animate');const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');io.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const story=document.querySelector('.scroll-story');let scheduled=false;function draw(){scheduled=false;if(motion.matches)return;const r=story.getBoundingClientRect();const progress=Math.max(0,Math.min(1,(innerHeight*.55-r.top)/Math.max(1,r.height-innerHeight*.45)));story.style.setProperty('--multi-scale',String(.86+.14*progress))}addEventListener('scroll',()=>{if(!scheduled){scheduled=true;requestAnimationFrame(draw)}},{passive:true});addEventListener('resize',draw);draw()}})();
